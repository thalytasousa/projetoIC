<h1>Usuários:</h1>

<table class="table">
  <%= for user <- @users do %>
      <tr>
        <td><%= render "user.html", user: user %></td>
        <td><%= link "Mais Detalhes", to: Routes.user_path(@conn, :show, user.id) %></td>
      </tr>
  <% end %>
</table>
